fastapi==0.115.0
uvicorn==0.30.6
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
pydantic>=2.0.0
python-multipart>=0.0.7
PyPDF2>=3.0.0

# NOTE:
# - This "lite" set intentionally excludes TensorFlow and SentenceTransformers
#   so the service can start on newer Python versions (e.g. 3.13) where TF wheels
#   may not be available. main.py will run in fallback mode without them.
